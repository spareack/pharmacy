version: '3.8'

services:
  web:
    build:
      context: .
    container_name: django_web
    command: >
      sh -c "poetry run python manage.py makemigrations && 
             poetry run python manage.py migrate &&
             poetry run gunicorn backend.wsgi:application --bind 0.0.0.0:8000"
    ports:
      - "8000:8000"
    environment:
      - DJANGO_SECRET_KEY=django-insecure-yj_4vwxwkz!f6)2ykfj6y0z-!^!hfddywb!w-osf4%mn)x46ri
      - DJANGO_ENV=Development
      - POSTGRES_USER=test
      - POSTGRES_PASSWORD=qweqwe
      - POSTGRES_DB=project_db
      - POSTGRES_HOST=db
      - POSTGRES_PORT=5432
    depends_on:
      - db
  db:
    image: postgres:15
    container_name: postgres_db
    volumes:
      - postgres_data:/var/lib/postgresql/data
    environment:
      POSTGRES_USER: ${POSTGRES_USER}
      POSTGRES_PASSWORD: ${POSTGRES_PASSWORD}
      POSTGRES_DB: ${POSTGRES_DB}
      POSTGRES_HOST: ${POSTGRES_HOST}
      POSTGRES_PORT: ${POSTGRES_PORT}
volumes:
  postgres_data:
